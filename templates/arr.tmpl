<!-- templates/arr.tmpl -->
<html>
<link rel="stylesheet" href="/assets/css/ginlike.css">
<body>
    <p>hello, {{.title}}</p>
    {{range $index, $ele := .stuArr }}
    <p>{{ $index }}: {{ $ele.Name }} is {{ $ele.Age }} years old</p>
    {{ end }}
</body>
</html>